# Browser Agent (Playwright + LLM)

Этот проект — простой браузерный агент, который:

- запускает Chromium с постоянным пользовательским профилем,
- собирает компактный DOM,
- передаёт DOM и задачу в LLM,
- выполняет шаги (клики, ввод текста, переходы), которые возвращает модель.

Используется для автоматизации действий в браузере.

---

##  Установка

### 1. Установить зависимости Python

```bash
pip install -r requirements.txt
```

### 2. Установить Playwright

```bash
playwright install
```

---

## ▶️ Запуск агента

```bash
python main.py
```

После запуска:

- откроется Chromium с постоянным профилем,
- агент будет ждать текстовую команду в терминале  
  *(например: «найди суп и добавь в корзину»)*.

---

## 📁 Структура проекта

```
(base) arslan@macbook-4 browser_agent % tree
.
├── README.md
├── __pycache__
│   └── config.cpython-311.pyc
├── actions
│   ├── __init__.py
│   ├── __pycache__
│   │   ├── __init__.cpython-311.pyc
│   │   └── actions.cpython-311.pyc
│   └── actions.py
├── agent
│   ├── __init__.py
│   ├── __pycache__
│   │   ├── __init__.cpython-311.pyc
│   │   ├── agent.cpython-311.pyc
│   │   └── llm.cpython-311.pyc
│   ├── agent.py
│   └── llm.py
├── browser
│   ├── __init__.py
│   ├── __pycache__
│   │   ├── __init__.cpython-311.pyc
│   │   ├── browser.cpython-311.pyc
│   │   └── dom.cpython-311.pyc
│   ├── browser.py
│   └── dom.py
├── config.py
├── main.py
└── requirements.txt

---

## Пользовательский профиль Chromium

Профиль браузера хранится в:

```
~/chromium-profile
```

Чтобы очистить данные браузера:

```bash
rm -rf ~/chromium-profile
```

---

## 📝 Пример задачи

После запуска:

```
Введите задачу: открой яндекс лавку и найди суп, добавь суп с фрикадельками в корзину
```

Агент проанализирует страницу и выполнит действия автоматически.

---

## 🔧 Требования

- Python 3.10+
- Playwright
- Claude API ключ 

---

##  Как работает агент

1. Chromium запускается с флагами:
   - `--remote-debugging-port=9222`
   - `--user-data-dir=...`

2. `extract_dom()` собирает:
   - url и title,
   - небольшой HTML-фрагмент,
   - кнопки, ссылки, инпуты,
   - результаты поиска (если присутствуют).

3. DOM + текст задачи отправляются в LLM.

4. Модель возвращает последовательность действий в формате JSON.

5. Агент пошагово выполняет эти действия с помощью Playwright.

---

##  Примечание

Проект служит демонстрацией базового браузерного агента.  
При необходимости можно расширить его:
- добавлением новых действий,
- поддержкой нескольких шагов мыши,
- интеграцией более умного планировщика,
- записью видео,
- прокси и куки-сессиями.

---

